name: Deploy to AWS S3
on:
  push:
    branches:
      - master
env:
  ENV: dev
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: print name
        run: |
          echo ${GITHUB_REF##*/}
